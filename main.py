from line_intersection.kernel import Point, LineSegment

if __name__ == "__main__":
    import argparse
    parser = argparse.ArgumentParser(
        description = "Compute line intersection using plane sweep and brute force")
